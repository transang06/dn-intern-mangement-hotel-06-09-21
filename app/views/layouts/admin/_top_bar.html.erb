<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #889EAF;">
  <div class="container-fluid ">
    <%= form_tag search_path, method: :get, class:"d-flex" do %>
      <%= search_field_tag :key, params[:key], class: "form-control col-4 me-2", placeholder: "Search", required: true %>
      <%= submit_tag t("other.search"), class: "btn btn-success" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav ">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= t "other.language" %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li>
              <%= link_to t("english"), {locale: :en}, {class: "dropdown-item"} %>
            </li>
            <li>
              <%= link_to t("vietnamese"), {locale: :vi}, {class: "dropdown-item"} %>
            </li>
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.last_name + "(#{current_user.role})" %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li>
              <%= link_to t("users.profile"), current_user, class: "dropdown-item" %>
            </li>
            <li>
              <%= link_to t("receipt.history"), receipts_path , class: "dropdown-item" %>
            </li>
            <li>
              <%= link_to t("users.log_out"), logout_path, method: :delete, class: "dropdown-item" %>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
