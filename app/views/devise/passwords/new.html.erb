<% provide(:title, t("users.forgot_pass")) %>
<div class="mx-auto my-3 form_w">
  <h1><%= t "users.forgot_pass" %></h1>
  <div class="row">
    <div class="col-md-offset-3">
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control my-2", required: true %>
        <%= f.submit t("users.send_pass"), class: "btn btn-primary my-4" %>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
